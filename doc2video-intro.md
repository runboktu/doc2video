兄弟们，你还在手动做视频？

打开 Word，复制文字，截图，找配图，打开剪辑软件，拖时间线，对字幕对到眼花，导出等了半小时发现字幕错位三个字——然后重来。

一篇文章做成视频，一个下午没了。

现在，想象另一种可能。你喝了一口咖啡，敲了一条命令，然后就没了。真的没了。你去上了个厕所回来，视频已经躺在桌面上，带字幕，带配音，带 AI 生成的配图。

你可能会问：这是魔法吗？

不是，这是一个叫 doctovideo 的工具。它的全部操作就是一条命令。

你给它一篇 Markdown 文章，它帮你干完所有脏活。

第一步，它把文章洗得干干净净。什么井号、星号、书名号、破折号，全部剃掉。就像给文章洗了个澡，只剩纯文本，干干净净交给语音引擎。

第二步，它把文章念出来。不是那种冷冰冰的机器声，是微软云的神经网络语音，有感情，有停顿，有节奏。念完还顺便把字幕时间戳精确到毫秒给你标好。这份字幕，后面要用的。

第三步，AI 出场。DeepSeek(也可指定其他AI) 读一遍你的文章，读一遍字幕时间线，然后告诉你：这里应该切一个镜头，那里应该来个特写，这段用远景，那段用近景。它输出的不是模糊建议，是一份精确到秒的分镜头脚本，JSON 格式，每个镜头有英文提示词、时长、负面排除词，整整齐齐。

第四步，另一个 AI 拿着这些提示词去画画。千问大模型，一张一张地生成。水墨风格、现代扁平、铅笔素描，你说了算。画完还不算完，每张画会被 FFmpeg 施一个叫 Ken Burns 的魔法——缓慢推拉摇移，静态图秒变电影感镜头。

第五步，所有镜头被拼接在一起，速度微调到和音频严丝合缝。然后音频叠上去。然后字幕烧进去。完事。

全程大概七分钟。其中六分半是 AI 在干活，你负责的那半分钟就是敲那一条命令。

让我算算你的投入产出比。一杯咖啡的时间，零剪辑技能要求，一条命令，换来一个带配音、带字幕、带 AI 配图的完整视频。

你以前花一整个下午干的活，现在就是一个回车键的距离。

说真的，这段文字你正在读的这段文字，它自己就是一个活生生的例子。它会被这个工具自己做成视频。一个工具做的视频，内容是介绍这个工具本身。套娃吗？有一点。但这也恰恰说明——它真的能处理任何文章。包括一篇介绍它自己的文章。


这款工具把每一步都拆成了独立的阶段。哪一步不满意，就从那一步重跑。图片不满意？直接从 gen_images 继续，不用从头再来。语音觉得怪？重跑 gen_audio 就行，前面洗好的文本还在。每一步的输出都是下一步的输入，中间结果全部保留，随时可以接续。

而且，生成音频和图片的脚本都是可以自定义的。默认用的是微软云语音和千问大模型，但你觉得哪个引擎更顺耳、哪个画风更对味，自己改配置就行。工具不锁死你，它给你留了口子。

所以，下次你想把一份文档做成视频的时候，别打开剪辑软件了。打开终端就好。
